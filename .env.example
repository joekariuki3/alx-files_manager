PORT=5000

# MongoDB connection
DB_HOST=localhost
DB_PORT=27017
DB_DATABASE=files_manager

# Local file storage path (ensure the process can write here)
FOLDER_PATH=/tmp/files_manager

# Redis uses defaults (localhost:6379) in this project.
# To customize Redis, extend utils/redis.js accordingly.
